Docker image based on https://hub.docker.com/_/openjdk (which run on port 8080).

Use the following command to start the container: docker run -p <port-on-your-machine>:8080 lschils/devopswithdocker-spring-example-project then access it in your browser on the port .
